services:
  # 1. MySQL service
  db:
    image: mysql:8.0
    container_name: edms-mysql
    restart: unless-stopped
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootsecret}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-edms}
      MYSQL_USER: ${MYSQL_USER:-edmsuser}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-edmspass}
    volumes:
      - ./data/mysql:/var/lib/mysql
    networks:
      - edms-network

  # Backend service
  backend:
    build:
      context: ../backend
      dockerfile: ../docker/backend/Dockerfile
    image: edms-backend:latest
    container_name: edms-backend
    restart: unless-stopped
    depends_on:
      - db
    volumes:
      # Persist database and uploaded files
      - ./data/backend:/app/data
    environment:
      - FLASK_ENV=production
      - DATABASE_URL=${DATABASE_URL}
      - STORAGE_PATH=/app/data
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-your-secret-key-change-in-production}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost}
      - SPARK_APPID=${SPARK_APPID}
      - SPARK_API_KEY=${SPARK_API_KEY}
      - SPARK_API_SECRET=${SPARK_API_SECRET}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - AI_IGNORE_PROXIES=${AI_IGNORE_PROXIES:-true}
    networks:
      - edms-network
    dns:
      - 223.5.5.5
      - 114.114.114.114
      - 8.8.8.8
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5000/api/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Frontend service
  frontend:
    build:
      context: ..
      dockerfile: docker/frontend/Dockerfile
      args:
        - FRONTEND_DIR=frontend
    image: edms-frontend:latest
    container_name: edms-frontend
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-80}:80"
      - "${SSL_PORT:-443}:443"  # SSL port for HTTPS access
    volumes:
      # To enable HTTPS, uncomment the lines below to mount the SSL Nginx config and your certificate files
      # - ./frontend/nginx.ssl.conf:/etc/nginx/conf.d/default.conf:ro
      # - ./certs:/etc/nginx/certs:ro
      - ./data/frontend:/var/log/nginx
    networks:
      - edms-network
    depends_on:
      - backend
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "--no-check-certificate", "http://127.0.0.1/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  edms-network:
    driver: bridge

volumes:
  data:
    driver: local
